<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===== COLORS ===== -->
    <SolidColorBrush x:Key="TabBg" Color="#F8FAFC"/>
    <SolidColorBrush x:Key="TabItemBg" Color="#E5EAF0"/>
    <SolidColorBrush x:Key="TabItemActiveBg" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="TabItemHoverBg" Color="#EEF2F7"/>
    <SolidColorBrush x:Key="TabBorder" Color="#E5E7EB"/>
    <SolidColorBrush x:Key="TabText" Color="#1F2937"/>


    <!-- ===== TABCONTROL ===== -->
    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource TabBg}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
    </Style>


    <!-- ===== TABITEM ===== -->
    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource TabText}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="14,6"/>
        <Setter Property="Margin" Value="2,2,4,2"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Bd"
                            Background="{StaticResource TabItemBg}"
                            BorderBrush="{StaticResource TabBorder}"
                            BorderThickness="0,0,0,1"
                            CornerRadius="6,6,6,6"
                            Padding="{TemplateBinding Padding}">

                        <ContentPresenter
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            ContentSource="Header"/>
                    </Border>

                    <ControlTemplate.Triggers>

                        <!-- SELECTED -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="{StaticResource TabItemActiveBg}"/>
                            <Setter TargetName="Bd" Property="BorderThickness"
                                    Value="1,1,1,0"/>
                            <Setter TargetName="Bd" Property="Panel.ZIndex"
                                    Value="10"/>
                        </Trigger>

                        <!-- HOVER -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="{StaticResource TabItemHoverBg}"/>
                        </Trigger>

                        <!-- DISABLED -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>