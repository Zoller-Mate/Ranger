<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===== COLORS ===== -->
    <SolidColorBrush x:Key="DG_Background" Color="#F7F9FB"/>
    <SolidColorBrush x:Key="DG_RowAlt" Color="#EEF2F7"/>
    <SolidColorBrush x:Key="DG_Foreground" Color="#1F2937"/>
    <SolidColorBrush x:Key="DG_Hover" Color="#E5EAF0"/>


    <!-- ===== DATAGRID ===== -->
    <Style TargetType="DataGrid">
        <!-- BEHAVIOR -->
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
        <Setter Property="CanUserSortColumns" Value="True"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="AutoGenerateColumns" Value="True"/>

        <!-- LOOK -->
        <Setter Property="Background" Value="{StaticResource DG_Background}"/>
        <Setter Property="Foreground" Value="{StaticResource DG_Foreground}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="HeadersVisibility" Value="Column"/>

        <!-- ROWS -->
        <Setter Property="AlternationCount" Value="2"/>

        <!-- COLUMNS -->
        <Setter Property="ColumnWidth" Value="*"/>
        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
    </Style>


    <!-- ===== ROW ===== -->
    <Style TargetType="DataGridRow">
        <Setter Property="Background" Value="{StaticResource DG_Background}"/>
        <Setter Property="Foreground" Value="{StaticResource DG_Foreground}"/>
        <Setter Property="Height" Value="34"/>
        <Setter Property="BorderThickness" Value="0"/>

        <Style.Triggers>
            <!-- Alternating row -->
            <Trigger Property="AlternationIndex" Value="1">
                <Setter Property="Background" Value="{StaticResource DG_RowAlt}"/>
            </Trigger>

            <!-- Hover -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource DG_Hover}"/>
            </Trigger>
        </Style.Triggers>
    </Style>


    <!-- ===== CELL ===== -->
    <Style TargetType="DataGridCell">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="10,4"/>
    </Style>


    <!-- ===== COLUMN HEADER ===== -->
    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="#F1F5F9"/>
        <Setter Property="Foreground" Value="#111827"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="BorderBrush" Value="#E5E7EB"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
    </Style>

</ResourceDictionary>