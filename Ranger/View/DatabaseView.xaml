<UserControl x:Class="Ranger.View.DatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ranger.View"
             xmlns:viewModel="clr-namespace:Ranger.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="1300">
    <UserControl.DataContext>
        <viewModel:DatabaseViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        <TabControl ItemsSource="{Binding Tables}"
                    SelectedItem="{Binding SelectedTable}"
                    x:Name="MainTabs">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding TableName}" 
                                   FontWeight="Bold" />
                </DataTemplate>
            </TabControl.ItemTemplate>

            <TabControl.ContentTemplate>
                <DataTemplate>
                    <DataGrid ItemsSource="{Binding Rows}"/>
                    <!--:DataGridAutoResizeBehavior.HostTab="{Binding ElementName=MainTabs}"/-->
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>

        <StackPanel Grid.Row="1"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="10"
            VerticalAlignment="Center">
            
            <Button Content="Refresh" Command="{Binding LoadDatabaseDumpCommand}" Style="{StaticResource SecondaryButton}"
            Margin="10,0,0,0"/>
            <Button Content="Save as" Command="{Binding DatabaseSaveAsCommand}" Style="{StaticResource PrimaryButton}"
            Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
